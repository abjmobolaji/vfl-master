{{> header}}
<div class="main-content">
    <div class="container">
        <h1>Hello World from Handlebars</h1>
        <p>This is home page</p>
    </div>
    <p>
        {{#if msg}}
            {{msg}}
        {{else}}
            No message was found
        {{/if}}
    </p>
</div>
{{> footer}}